[tool.poetry]
name = "ynified"
version = "0.1.0"
description = ""
authors = ["palee"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
bson = "^0.5.10"
coloredlogs = "^15.0.1"
pyyaml = "^6.0.2"
pygments = "^2.19.1"
ipaddress = "^1.0.23"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
